<h1>Eat Da Burger</h1>

<h2>Burgers To Eat</h2>
<ul>
    {{!-- We need to loop through all of the burgers --}}
        {{!-- If the burgers are not devoured --}}
        {{!-- Then they appear here with a devour button next to it --}}

    <h3>These need eaten!</h3>
    {{#each burgers}} 
        {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
            {{!-- Devour Button --}}
            {{> burgers/devour-burger}}
        {{/unless}}
    {{/each}}
</ul>

<h2>These Were Tasty!</h2>
<ul>
    {{!-- We need to loop through all of the burgers again --}}
        {{!-- If the burgers are devoured --}}
        {{!-- Then they appear over here without a button and grayed out --}}
    {{#each burgers}}
        {{#if devoured}}
            
            {{> burgers/burger-block devoured=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add A Burger</h2>
<form action="/create" class="new_burger-form" method="POST">
    <div class="form-group">
        <label for="burger_name">What Burger Would You Like to Add?</label>
        <input type="text" id="burger_name" name="burger_name" placeholder="Add Burger Here">
    </div>
    <button id="addBurgerButton" type="submit">Add Burger</button>
</form>